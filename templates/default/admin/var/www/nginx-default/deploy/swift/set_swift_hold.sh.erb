#Sets all swift packages to state "HOLD" so apt-get upgrades do not automatically push swift upgrades without explicity

dpkg -l | grep swift | awk {'print $2'} | while read line; do echo "$line hold" | dpkg --set-selections; done;
