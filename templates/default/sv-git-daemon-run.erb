#!/bin/sh
exec 2>&1
echo 'git-daemon starting'
exec chpst -ugitdaemon \
  "$(git --exec-path)"/git-daemon --verbose --reuseaddr --syslog --max-connections=0 \
    --export-all --base-path=<%= @options[:base_path] %> <%= @options[:base_path] %>
