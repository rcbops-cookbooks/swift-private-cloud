# By default it will send mail to the swiftops alias below
# which in turn redirects it to the proper location 
#MAILTO="swiftops"
# Calls the memcache check script to make sure it is still
# responding properly to requests/connections
#*/2 * * * * root /bin/bash /usr/local/bin/check_memcache_wrapper.sh
