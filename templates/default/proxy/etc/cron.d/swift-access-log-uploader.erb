# Log processing configuration file that is used by the 
# python-slogging tools for parsing and storing the swift proxy
# access logs. That later can be used for other things like
# access-log-delivery, a billing parser, stats parser ... etc 
#
#10 * * * * swift swift-log-uploader /etc/swift/log-processor.conf access
